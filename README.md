# Syncfy 🎵

Un clon premium de Spotify con un diseño en modo oscuro y acentos en Amarillo Syncfy (#FFC212).

## Características

- 🎨 **Diseño Premium**: Interfaz moderna, minimalista y "glassmorphism".
- 📱 **PWA (Progressive Web App)**: Instalable en escritorio y móvil.
- 🟡 **Tema Personalizado**: Color de acento único `#FFC212`.
- ⚡ **Ligero**: Construido con Vanilla JS, HTML y CSS. Sin dependencias pesadas.

## Cómo ejecutar localmente

1. Abre el archivo `index.html` en tu navegador.
2. Para probar la funcionalidad PWA completa (Service Workers), es recomendable usar un servidor local (ej. Live Server en VS Code o `npx serve`).

## Despliegue en GitHub Pages

1. Sube este repositorio a GitHub.
2. Ve a **Settings** > **Pages**.
3. Selecciona la rama `main` (o `master`) y la carpeta `/root`.
4. ¡Listo! Tu clon estará online en segundos.

## Personalización

Puedes cambiar los colores en `style.css` modificando las variables CSS en `:root`.
